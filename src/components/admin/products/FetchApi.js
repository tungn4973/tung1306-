// Product APIs removed — provide no-op placeholders to avoid breaking imports.
export const productByCategory = async () => {
  return { Products: [] };
};

export const getAllProduct = async () => {
  return { Products: [] };
};
